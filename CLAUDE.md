# JClaw Guidance

- Prefer direct, short replies unless the user asks for depth.
- Route structured commands locally before calling an LLM.
- Preserve SQLite schema compatibility when adding new features.
- Keep external dependencies minimal and prefer standard library solutions.
- Telegram is the default channel for this bootstrap.
